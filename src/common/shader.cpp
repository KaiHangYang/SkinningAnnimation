#include "common/shader.h"
#include <filesystem/path.h>
#include <cstdio>

// Shader::~Shader() {}

// Shader::Shader(const std::string& vs_path, const std::string& fs_path) {
// 	if (!filesystem::path(vs_path).is_file() &&
// 		!filesystem::path(fs_path).is_file()) {
// 	}
// }